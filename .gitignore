# yano-orchestrator: per-task git worktrees
.worktrees/
# yano-orchestrator: per-instance debug event logs, diagnostic only
logs/
node_modules/
# real secrets — NEVER commit (see docs/development-notes.md: a real Evolution API
# key + phone number were found committed in a sibling deployment's .env; this repo's
# history starts clean specifically to avoid repeating that mistake)
.env
# SQLite ticket/DAG storage + runtime data dirs, per-project/per-machine, never shared (Revisione 26+)
*.db
*.db-journal
.pi/
temp/
.DS_Store
