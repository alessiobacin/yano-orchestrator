# Host-native development broker. Keep this file separate from the Docker
# configuration: Docker must listen on 0.0.0.0 inside its own namespace, while
# a native Mosquitto process must remain loopback-only.
listener 1883 127.0.0.1
protocol mqtt
allow_anonymous true
persistence false
log_type error
log_type warning
log_type notice
