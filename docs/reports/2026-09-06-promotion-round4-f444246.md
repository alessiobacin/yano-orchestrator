# Promozione Round 4 — performance-optimization-loop (2026-09-06)

- **Run**: 01M1MF74R67KXXWDVQQJREBQJS
- **Playbook**: performance-optimization-loop (bind 19b7f3fdb79d…)
- **Candidate promosso**: `f444246` — `perf: typed fast-path in trace record reads`
- **Worktree candidate**: `.worktrees/performance-optimization-yano` (branch `task/performance-optimization-yano`)
- **Baseline-original immutabile**: `.worktrees/perf-baseline-original` @ `253456f` (mai modificata)
- **Nuova reference baseline**: `f444246` (per il Round 5)

## Cosa cambia

- `scripts/yano-trace-storage.mjs` (+37): parametro opzionale `type`/`types` in `readTraceRecords` — **substring pre-check** (`line.includes(JSON.stringify(candidate))`) prima del `JSON.parse` per le righe candidate, filtro esatto `item.type ===` invariato dopo il parse. Senza parametro: ciclo bit-identico. Falsi positivi possibili (rimossi dal filtro esatto), **mai falsi negativi** (la forma quotata `"type":"<valore>"` è sottostringa del serializzato se `item.type === candidate`; escaping identico del writer).
- `scripts/watch-stalls.mjs` (2 call site, righe ~1108/1119): le letture since-less full-corpus (`limit:100000`) per i tipi `context_compaction_completed`/`context_usage` ora passano il tipo al read invece di leggere tutto e filtrare in-memory.

## Perché

Il watcher a ogni ciclo (default 5 min) leggeva TUTTO il corpus trace (parse JSON di ogni riga) solo per trovare i record `context_compaction_completed`/`context_usage`, con costo lineare nel volume. Il fast path tipizzato evita il parse delle righe non candidate.

## Evidenze (Round 4)

- **Misura (harness `/tmp/yano-r4-harness.mjs`, blocco watcher reale, corpus N=50.000, 5 processi/lato, baseline `1565726` vs candidate `f444246`)**:
  - Cold: **224,19 → 131,50 ms (−41,3%)** · Warm: **193,02 → 122,76 ms (−36,4%)** → **`promote_large_gain`** (Δ ≥ 3%).
- **QA/qualità**: 6/6 smoke PASS (trace, trace-index, trace-memory, watcher-e2e, watch-stalls 16 assert, recovery); check-syntax OK; check:docs PASS; lint:playbooks 24/24.
- **Equivalenza**: letture senza tipo bit-identiche (50.000/50.000); typed read == filtro manuale per entrambi i tipi (7.143/7.143); type+since 103/103; type+limit monotono (100⊂500⊂100000 — superset, mai meno record rilevanti); harness adversarial (decoy, type più lungo, malformed) 11/11 PASS.
- **Concurrency**: skip motivato (read-only, nessun mutex/scrittura nuova; concorrenza watcher coperta da smoke e2e).
- **Contesto/token**: `unknown` (modifica tempo-CPU/IO). **Costo**: +0%.

## Decisione

Δ ≥ 3% → promozione automatica (`promote_large_gain`); nessuna decisione umana richiesta. `promote` (gen 26→27) e `next_round` (gen 27→28) applicati dal planner.

## Contatori loop aggiornati

- **Stagnazione (≤1%)**: 0/5 (serie interrotta)
- **Plateau (>1% e <3%)**: 0/3
- **Promozioni**: 4 (7ad9b35, ca4c512, a1e5f62, f444246) — nuova reference baseline = `f444246`

## Prossimo round

Round 5: nuova ipotesi di bottleneck misurabile nell'harness, da selezionare in `bottleneck_selected` (gen 28).