# yano data

Mostra o migra la directory dati globale di Yano.

~~~bash
yano data path
yano data migrate --dry-run
yano data migrate --yes --merge
~~~

Usare il percorso restituito da data path per trovare trace, indice semantico,
snapshot e registro Gantt.
