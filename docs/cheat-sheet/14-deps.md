# yano deps

Controlla dipendenze, skill, CLI e MCP richiesti dall’infrastruttura del progetto.

~~~bash
yano deps --project-root "$PWD"
yano deps --project-root "$PWD" --json
~~~

Usarlo prima di avviare un playbook importato o creato dall’architect.
