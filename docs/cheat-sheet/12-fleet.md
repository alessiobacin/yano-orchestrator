# yano fleet

Mostra gli agenti Herdr live associati a un progetto.

~~~bash
yano fleet --project-root "$PWD"
yano fleet --project-root "$PWD" --json
yano fleet --project sales-companion --json
~~~

È una vista operativa degli agenti, complementare a yano projects che elenca
anche i worker esterni.
