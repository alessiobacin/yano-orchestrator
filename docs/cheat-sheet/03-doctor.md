# yano doctor

Verifica i prerequisiti locali e, con --network, anche broker e collegamenti
necessari.

~~~bash
yano doctor
yano doctor --network
yano doctor --json
~~~

In caso di variabile mancante il comando indica il nome della variabile e il
comando yano config set da usare.
